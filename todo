[x] update exit status if input is not an executable command
    xxx
    > file
    echo $?

[ ] check ((cmd && cmd)) bash behavior (exit status + error message if any)

[x] syntax check in groups (doing: TERESA) + memory

[x] redirection categorization within command groups