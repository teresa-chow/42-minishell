Not working as expected:

[ ] export a="ola '$USER'"
    echo $a
[ ] (scrambling error msgs on simultaneous processes) yyy | xxx | echo ola!
[ ] (error message missing: invalid identifier) export +=

TERESA (vou resolver estes e as leaks abaixo)
[ ] fix prompt (no tilde)
[ ] ls | cat<<a>b (should read from heredoc, and is reading from pipe)

LEAKS:
[ ] fail && (echo && echo)
[ ] fail || (echo && echo)
[ ] (ls fail | echo ola && (echo a && echo b) || echo c)
[ ] (echo a | echo b | echo c && echo d) && echo e -- pipeline inside of group
            (likely cause: recursive syntax analysis in group)

------------------------------------------------------------------------------------------

Quem se despachar primeiro:
[ ] env -i ./minishell + minishell (second call forward prints color prompt without XTERM)

------------------------------------------------------------------------------------------

Testar e apontar erros !